rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // All files are publicly readable so stored URLs work without auth.
    // All writes go through the Admin SDK server-side and bypass these rules.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
